#include <bits/stdc++.h>
using namespace std;
#define lli long long int
#define nl << "\n"

int main()
{
    double y_;
    double x_ = -80;
    y_ = -2 * 1000000 / (-80 * -80 + 10000) + 380;
    cout << y_ << endl;
    y_ = -2 * 1000000 / (x_ * x_ + 10000) + 380;
    cout << y_ << endl;
}